- types = ['', 'action', 'success', 'warning', 'danger', 'info', 'clean']
- sizes = ['smallest', 'small', 'big', 'biggest']
- widths = ['wide', 'wider']

h1 Buttons

hr

ul.examples
  - for type in types
    li
      label .#{type}

      .example
        button(class=type) Submit

        | 

        button(class=type disabled=true) Disabled

ul.examples
  - for size in sizes
    li
      label .#{size}

      .example
        button(class=size) Submit

ul.examples
  - for width in widths
    li
      label .#{width}

      .example
        button(class=width) Submit

label .widest

button.widest Submit

